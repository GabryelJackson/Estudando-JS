// String. number, undefined, null, boolean

const nome = 'Gabryel' // String
const sobrenome = "Jacksoon" // String
const sobrenome1 = `Sacramento` // String
const numero = 10 // number
const numero2 = 10.52 // number
let nomeAluno; // undefined = não aponta pra local nenhum da memória
let sobrenomeAluno = null; // Nulo -> Não aponta para local nenhum na memória
const aprovado = false // Boolean = true, false (lógico)

console.log(typeof aprovado, aprovado)